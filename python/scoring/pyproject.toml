[project]
name = "crosswalk-scoring"
version = "0.1.0"
description = "Heuristic crosswalk scoring library"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
